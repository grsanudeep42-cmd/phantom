# Core AI — install ALL, only the one you configure is actually used
anthropic>=0.28.0    # for Anthropic (claude-*)
openai>=1.30.0       # for OpenAI, Ollama, OpenRouter, and any OpenAI-compat endpoint

# CLI + UI
click>=8.1.0
rich>=13.7.0
python-dotenv>=1.0.0

# Templating (reports)
jinja2>=3.1.0

# Identity
faker>=24.0.0
aiohttp>=3.9.0
aiosmtpd>=1.4.4       # only needed for self-hosted email

# Dev / testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
